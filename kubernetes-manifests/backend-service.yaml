# services/backend-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: frappe
spec:
  selector:
    app: backend
  ports:
  - port: 8000
    targetPort: 8000
    name: http