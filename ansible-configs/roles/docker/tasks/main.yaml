- name: Install docker
  apt:
    name:
      - docker.io
      - docker-compose-plugin
    update_cache: yes

- name: Enable docker
  systemd:
    name: docker
    state: started
    enabled: yes

- name: Add ubuntu to docker group
  user:
    name: ubuntu
    groups: docker
    append: yes